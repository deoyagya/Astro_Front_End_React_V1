# API Base URL — auto-detected for all platforms. Leave empty for local dev.
# The app auto-discovers your Mac's address from the Expo dev server connection.
# Only set this for production or to manually override:
#   EXPO_PUBLIC_API_BASE_URL=https://api.yourdomain.com
#
EXPO_PUBLIC_API_BASE_URL=
